# Puerto del servidor (por defecto 3000)
PORT=3000

# URI de MongoDB local (por defecto sin auth)
MONGO_URI=mongodb://127.0.0.1:27017/centrobet

# Si usas MongoDB con usuario/contraseña:
# MONGO_URI=mongodb://usuario:contraseña@127.0.0.1:27017/centrobet
